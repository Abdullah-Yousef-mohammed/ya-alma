package com.yaalma.api.repositories;

import com.yaalma.api.models.LanguageCenter;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LanguageCenterRepository extends JpaRepository<LanguageCenter, Long> {
}
